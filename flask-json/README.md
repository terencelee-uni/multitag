# Basic JSON output Flask App

Simple flask app that outputs a json with the predicted tags given an image. Run script.py after starting up the flask app (FLASK_ENV=development FLASK_APP=app.py flask run)
